## アーキテクチャ(分割ファイルの例)

このスライドは `![[sections/architecture.md]]` で親デッキに埋め込まれている。

```pane
+----------------------------------+
|  flow                            |
+----------------------------------+
|  desc                            |
+----------------------------------+
```

::: pane flow
`Markdown → AST → Deck IR → Scene Graph → HTML / PDF`
:::

::: pane desc
- コアは Rust、エディタ・ブラウザへは WASM で同一エンジンを配布
- レイアウトはペインの矩形まで、テキスト組版はブラウザに委譲
:::
